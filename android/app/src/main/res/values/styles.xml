<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.EdgeToEdge.Light">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#000000</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">true</item> <!-- dark icons -->
        <item name="android:windowLightNavigationBar">true</item> <!-- dark icons -->
        <item name="android:windowSoftInputMode">adjustResize</item>
        <!-- <item name="enforceNavigationBarContrast">false</item> -->
    </style>

</resources>
